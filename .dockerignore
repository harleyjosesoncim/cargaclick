# Ignora diretório do Git (não deve ir para a imagem)
.git/

# Ignora configurações locais de bundler
.bundle/

# Ignora variáveis de ambiente sensíveis
.env*
!/.env*.erb  # exceto templates de exemplo

# Ignora arquivos de chave de criptografia
/config/master.key
/config/credentials/*.key

# Ignora arquivos de log e temp
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# Ignora arquivos PID, mantendo diretório
/tmp/pids/*
!/tmp/pids/.keep

# Ignora uploads locais e bancos SQLite
/storage/*
!/storage/.keep
/tmp/storage/*
!/tmp/storage/.keep

# Ignora arquivos temporários do editor
*.swp
*.swo

# Ignora builds locais de assets (serão regenerados)
node_modules/
app/assets/builds/*
!app/assets/builds/.keep

# Ignora assets públicos pré-compilados
/public/assets/*
!/public/assets/.keep

# Ignora arquivos do sistema operacional/macOS
.DS_Store
Thumbs.db
