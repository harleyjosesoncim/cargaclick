class CreateAdmins < ActiveRecord::Migration[7.1]
  def change
    create_table :admins do |t|
      t.string :nome
      t.string :email, null: false, index: { unique: true }
      t.string :telefone
      t.string :cargo, default: "admin"
      t.string :password_digest # se usar has_secure_password

      t.timestamps
    end
  end
end
