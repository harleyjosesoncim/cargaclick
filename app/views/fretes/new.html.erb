<!-- app/views/fretes/new.html.erb -->
<% content_for :title, "Nova Solicitação de Frete" %>

<%= render 'shared/errors', object: @frete %>

<%= form_with(model: @frete, local: true, html: { class: 'max-w-2xl mx-auto mt-10' }) do |form| %>
  <div class="bg-white rounded-3xl shadow-2xl p-6 sm:p-8 border border-blue-200">
    <div class="flex items-center space-x-4 mb-6 pb-4 border-b border-gray-200">
      <span class="text-4xl sm:text-5xl text-blue-600">📦</span>
      <h1 class="text-2xl sm:text-3xl font-extrabold text-gray-900 leading-tight">
        Nova Solicitação de Frete
      </h1>
    </div>

    <div class="space-y-5">
      <!-- Origem -->
      <div class="flex flex-col">
        <%= form.label :origem, "Origem (CEP)", class: "block text-sm font-medium text-gray-700 mb-1" %>
        <%= form.text_field :origem,
                            placeholder: "Ex: 18000-000",
                            class: "w-full rounded-lg border-gray-300 shadow-sm p-2 sm:p-3 text-gray-800 focus:ring-2 focus:ring-blue-500",
                            pattern: "\\d{5}-?\\d{3}",
                            required: true,
                            aria: { required: true } %>
      </div>

      <!-- Destino -->
      <div class="flex flex-col">
        <%= form.label :destino, "Destino (CEP)", class: "block text-sm font-medium text-gray-700 mb-1" %>
        <%= form.text_field :destino,
                            placeholder: "Ex: 12892-634",
                            class: "w-full rounded-lg border-gray-300 shadow-sm p-2 sm:p-3 text-gray-800 focus:ring-2 focus:ring-blue-500",
                            pattern: "\\d{5}-?\\d{3}",
                            required: true,
                            aria: { required: true } %>
      </div>

      <!-- Descrição -->
      <div class="flex flex-col">
        <%= form.label :descricao, "Descrição da Carga", class: "block text-sm font-medium text-gray-700 mb-1" %>
        <%= form.text_area :descricao,
                           placeholder: "Ex: Caixas, móveis desmontados, etc.",
                           rows: 3

