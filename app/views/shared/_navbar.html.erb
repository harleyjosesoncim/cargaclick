<!-- app/views/shared/_navbar.html.erb -->
<nav class="bg-blue-600 dark:bg-gray-900 shadow-md">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16 items-center">

      <!-- Logo -->
      <div class="flex-shrink-0">
        <%= link_to "CargaClick 🚛", root_path, class: "text-xl font-bold text-white hover:text-yellow-300 transition-colors" %>
      </div>

      <!-- Links de navegação (Desktop) -->
      <div class="hidden md:flex space-x-6 items-center">
        <%= link_to "Clientes", clientes_path, class: "text-white hover:text-yellow-300 transition-colors" %>
        <%= link_to "Transportadores", transportadores_path, class: "text-white hover:text-yellow-300 transition-colors" %>
        <%= link_to "Fretes", fretes_path, class: "text-white hover:text-yellow-300 transition-colors" %>
        <%= link_to "Novo Frete", new_frete_path, class: "bg-yellow-400 text-blue-900 font-semibold px-3 py-1 rounded-lg hover:bg-yellow-500 transition" %>
        <%= link_to "Minhas Cotações", cotacoes_path, class: "text-white hover:text-yellow-300 transition-colors" %>
        <%= link_to "Administração", admin_root_path, class: "text-white hover:text-yellow-300 transition-colors" %>
      </div>

      <!-- Autenticação (Desktop) -->
      <div class="hidden md:flex items-center space-x-4">
        <% if cliente_signed_in? %>
          <span class="text-white">👋 <%= current_cliente.nome.presence || current_cliente.email %></span>
          <%= link_to "Sair", destroy_cliente_session_path, method: :delete, 
                class: "bg-red-500 text-white px-3 py-1 rounded-lg hover:bg-red-600 transition" %>
        <% else %>
          <%= link_to "Entrar", new_cliente_session_path, 
                class: "text-white hover:text-yellow-300 transition-colors" %>
          <%= link_to "Registrar", new_cliente_registration_path, 
                class: "bg-green-500 text-white px-3 py-1 rounded-lg hover:bg-green-600 transition" %>
        <% end %>
      </div>

      <!-- Botão Mobile -->
      <div class="md:hidden">
        <button id="menu-button" class="text-white focus:outline-none text-2xl">
          ☰
        </button>
      </div>
    </div>
  </div>

  <!-- Menu Mobile -->
  <div id="mobile-menu" class="hidden md:hidden px-4 pb-4 space-y-2 bg-blue-700 dark:bg-gray-800">
    <%= link_to "Clientes", clientes_path, class: "block text-white hover:text-yellow-300 transition-colors" %>
    <%= link_to "Transportadores", transportadores_path, class: "block text-white hover:text-yellow-300 transition-colors" %>
    <%= link_to "Fretes", fretes_path, class: "block text-white hover:text-yellow-300 transition-colors" %>
    <%= link_to "Novo Frete", new_frete_path, class: "block bg-yellow-400 text-blue-900 font-semibold px-3 py-1 rounded-lg hover:bg-yellow-500 transition" %>
    <%= link_to "Minhas Cotações", cotacoes_path, class: "block text-white hover:text-yellow-300 transition-colors" %>
    <%= link_to "Administração", admin_root_path, class: "block text-white hover:text-yellow-300 transition-colors" %>

    <hr class="border-gray-600 my-2">

    <% if cliente_signed_in? %>
      <span class="block text-white">👋 <%= current_cliente.nome.presence || current_cliente.email %></span>
      <%= link_to "Sair", destroy_cliente_session_path, method: :delete, 
            class: "block bg-red-500 text-white px-3 py-1 rounded-lg hover:bg-red-600 transition" %>
    <% else %>
      <%= link_to "Entrar", new_cliente_session_path, 
            class: "block text-white hover:text-yellow-300 transition-colors" %>
      <%= link_to "Registrar", new_cliente_registration_path, 
            class: "block bg-green-500 text-white px-3 py-1 rounded-lg hover:bg-green-600 transition" %>
    <% end %>
  </div>
</nav>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    const btn = document.getElementById("menu-button");
    const menu = document.getElementById("mobile-menu");
    if (btn) {
      btn.addEventListener("click", () => {
        menu.classList.toggle("hidden");
      });
    }
  });
</script>
