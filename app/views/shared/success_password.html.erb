<div class="min-h-screen flex items-center justify-center bg-gradient-to-br from-green-50 to-blue-100 p-8">
  <div class="bg-white rounded-3xl shadow-2xl p-10 max-w-md text-center border border-green-200">
    <div class="text-6xl mb-4 text-green-600">✅</div>
    <h1 class="text-3xl font-bold text-green-700 mb-4">Senha alterada com sucesso!</h1>
    <p class="text-gray-600 mb-6">Agora você já pode acessar sua conta normalmente.</p>

    <div class="flex flex-col gap-4">
      <%= link_to 'Ir para o Login', new_session_path(resource_name), class: 'bg-blue-500 hover:bg-blue-600 text-white font-semibold px-6 py-3 rounded-xl transition' %>
      <%= link_to 'Voltar ao Início', root_path, class: 'bg-gray-100 hover:bg-gray-200 text-gray-700 font-semibold px-6 py-3 rounded-xl transition' %>
    </div>
  </div>
</div>
