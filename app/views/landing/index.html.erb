<div class="min-h-[75vh] flex items-center justify-center px-4 py-10">
  <div class="w-full max-w-5xl">

    <!-- Logo + título -->
    <div class="text-center mb-10">
      <div class="mx-auto w-24 h-24 rounded-2xl bg-purple-700 flex items-center justify-center shadow-lg">
        <span class="text-white text-3xl font-extrabold">CC</span>
      </div>
      <h1 class="mt-4 text-3xl md:text-4xl font-extrabold tracking-tight">
        CargaClick
      </h1>
      <p class="mt-2 text-gray-600">
        Mobile-first. Sem botões perdidos. Offline-safe.
      </p>
    </div>

    <!-- Cards principais -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-4">

      <!-- CLIENTE -->
      <div class="bg-white rounded-2xl shadow p-5">
        <h2 class="text-xl font-bold">Cliente</h2>
        <p class="text-sm text-gray-600 mt-1">
          Entrar, cadastrar e simular em poucos toques.
        </p>

        <div class="mt-4 flex flex-col gap-2">
          <%= link_to "Entrar (Cliente)",
                new_cliente_session_path,
                class: "w-full text-center px-4 py-2 rounded bg-purple-700 text-white hover:bg-purple-800" %>

          <%= link_to "Cadastrar (Cliente)",
                new_cliente_registration_path,
                class: "w-full text-center px-4 py-2 rounded bg-gray-100 hover:bg-gray-200" %>

          <%= link_to "Painel do Cliente",
                clientes_root_path,
                class: "w-full text-center px-4 py-2 rounded bg-gray-50 hover:bg-gray-100" %>
        </div>
      </div>

      <!-- TRANSPORTADOR (SEM DEVISE DIRETO) -->
      <div class="bg-white rounded-2xl shadow p-5">
        <h2 class="text-xl font-bold">Transportador</h2>
        <p class="text-sm text-gray-600 mt-1">
          Cadastrar, receber oportunidades e acompanhar.
        </p>

        <div class="mt-4 flex flex-col gap-2">
          <%= link_to "Área do Transportador",
                transportador_path,
                class: "w-full text-center px-4 py-2 rounded bg-purple-700 text-white hover:bg-purple-800" %>

          <%= link_to "Cadastro do Transportador",
                transportador_path,
                class: "w-full text-center px-4 py-2 rounded bg-gray-100 hover:bg-gray-200" %>

          <%= link_to "Painel do Transportador",
                transportadores_root_path,
                class: "w-full text-center px-4 py-2 rounded bg-gray-50 hover:bg-gray-100" %>
        </div>
      </div>

      <!-- AÇÕES RÁPIDAS -->
      <div class="bg-white rounded-2xl shadow p-5">
        <h2 class="text-xl font-bold">Ações rápidas</h2>
        <p class="text-sm text-gray-600 mt-1">
          Sempre com Início / Voltar. Sem looping.
        </p>

        <div class="mt-4 flex flex-col gap-2">
          <%= link_to "Simular Frete",
                simular_frete_path,
                class: "w-full text-center px-4 py-2 rounded bg-yellow-400 hover:bg-yellow-500 font-semibold" %>

          <%= link_to "Fidelidade",
                fidelidade_path,
                class: "w-full text-center px-4 py-2 rounded bg-gray-100 hover:bg-gray-200" %>

          <%= link_to "Contato",
                contato_path,
                class: "w-full text-center px-4 py-2 rounded bg-gray-100 hover:bg-gray-200" %>

          <%= link_to "Admin",
                "/admin",
                class: "w-full text-center px-4 py-2 rounded bg-gray-900 text-white hover:bg-black" %>
        </div>
      </div>
    </div>

    <!-- Rodapé -->
    <div class="mt-10 text-center text-xs text-gray-500">
      <p>
        Offline-safe: sem Alpine, Tailwind CDN ou Leaflet em runtime.
      </p>
    </div>

  </div>
</div>
