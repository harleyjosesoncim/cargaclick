<% content_for :title, "CargaClick — Início" %>

<div class="min-h-screen bg-slate-950 text-slate-100 flex flex-col">

  <!-- HEADER SIMPLES -->
  <header class="w-full px-4 py-4 flex justify-between items-center border-b border-slate-800">
    <h1 class="text-lg font-bold text-purple-300">CargaClick</h1>

    <div class="space-x-2 text-sm">
      <%= link_to "Entrar", new_cliente_session_path,
            class: "px-3 py-1 rounded bg-purple-700 hover:bg-purple-600" %>
      <%= link_to "Cadastrar", new_cliente_session_path,
            class: "px-3 py-1 rounded border border-purple-600 hover:bg-purple-800" %>
    </div>
  </header>

  <!-- CONTEÚDO CENTRAL -->
  <main class="flex-1 px-4 py-10 max-w-4xl mx-auto space-y-10">

    <!-- HERO -->
    <section class="text-center space-y-4">
      <h2 class="text-3xl font-extrabold">
        Fretes rápidos e sem intermediários
      </h2>

      <p class="text-slate-300 text-sm">
        Simule, contrate e acompanhe seus fretes em poucos cliques.
      </p>

      <%= link_to "Simular Frete",
            new_frete_path,
            class: "inline-block mt-4 px-6 py-3 rounded bg-yellow-500 text-black font-semibold hover:bg-yellow-400" %>
    </section>

    <!-- PERFIS -->
    <section class="grid grid-cols-1 sm:grid-cols-2 gap-6">

      <!-- CLIENTE -->
      <div class="p-6 rounded-xl bg-slate-900 border border-slate-800 text-center space-y-3">
        <div class="text-3xl">👤</div>
        <h3 class="font-semibold">Cliente</h3>

        <% if cliente_signed_in? %>
          <%= link_to "Acessar painel",
                cliente_path(current_cliente),
                class: "block px-4 py-2 rounded bg-blue-600 hover:bg-blue-500" %>
        <% else %>
          <%= link_to "Login / Cadastro",
                new_cliente_session_path,
                class: "block px-4 py-2 rounded bg-blue-600 hover:bg-blue-500" %>
        <% end %>
      </div>

      <!-- TRANSPORTADOR -->
      <div class="p-6 rounded-xl bg-slate-900 border border-slate-800 text-center space-y-3">
        <div class="text-3xl">🚚</div>
        <h3 class="font-semibold">Transportador</h3>

        <% if transportador_signed_in? %>
          <%= link_to "Acessar painel",
                transportador_path(current_transportador),
                class: "block px-4 py-2 rounded bg-green-600 hover:bg-green-500" %>
        <% else %>
          <%= link_to "Login / Cadastro",
                new_transportador_session_path,
                class: "block px-4 py-2 rounded bg-green-600 hover:bg-green-500" %>
        <% end %>
      </div>

    </section>

    <!-- LINKS SECUNDÁRIOS -->
    <section class="grid grid-cols-2 sm:grid-cols-4 gap-4 text-sm text-center">
      <%= link_to "Fidelidade", fidelidade_path, class: "p-3 rounded bg-slate-800 hover:bg-slate-700" %>
      <%= link_to "Relatórios", relatorios_path, class: "p-3 rounded bg-slate-800 hover:bg-slate-700" %>
      <%= link_to "Sobre", about_path, class: "p-3 rounded bg-slate-800 hover:bg-slate-700" %>
      <%= link_to "Contato", contato_path, class: "p-3 rounded bg-slate-800 hover:bg-slate-700" %>
    </section>

  </main>

  <!-- FOOTER -->
  <footer class="text-center text-xs text-slate-500 py-4 border-t border-slate-800">
    © <%= Time.current.year %> CargaClick
  </footer>

</div>
