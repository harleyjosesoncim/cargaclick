<div class="min-h-screen flex items-center justify-center bg-gradient-to-br from-blue-50 to-indigo-100 p-4">
  <div class="bg-white rounded-3xl shadow-2xl p-8 sm:p-10 max-w-md w-full border border-blue-200">
    
    <div class="flex items-center justify-center mb-6">
      <div class="text-4xl text-blue-600">🔑</div>
    </div>

    <h2 class="text-2xl font-bold text-center text-blue-700 mb-6">Esqueci minha senha</h2>

    <%= form_with(url: cliente_password_path, method: :post, scope: :cliente, local: true, class: "space-y-6") do |f| %>
      
      <% if flash[:notice] %>
        <div class="bg-green-100 text-green-700 px-4 py-2 rounded text-sm">
          <%= flash[:notice] %>
        </div>
      <% elsif flash[:alert] %>
        <div class="bg-red-100 text-red-700 px-4 py-2 rounded text-sm">
          <%= flash[:alert] %>
        </div>
      <% end %>

      <div>
        <%= f.label :email, "E-mail de recuperação", class: "block text-sm font-medium text-gray-700 mb-1" %>
        <%= f.email_field :email, required: true, autofocus: true,
            class: "w-full border rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500" %>
      </div>

      <div>
        <%= f.submit "Enviar instruções", class: "w-full bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded shadow" %>
      </div>

    <% end %>

    <div class="mt-6 text-center text-sm text-gray-600">
      <%= link_to "Voltar para login", new_cliente_session_path, class: "text-blue-600 hover:underline font-medium" %>
    </div>
  </div>
</div>
