<div class="min-h-screen flex items-center justify-center bg-gradient-to-br from-green-50 to-green-100 p-4">
  <div class="bg-white rounded-3xl shadow-2xl p-8 sm:p-10 max-w-md w-full border border-green-200">

    <div class="flex items-center justify-center mb-6">
      <div class="text-4xl text-green-600">🔑</div>
    </div>

    <h2 class="text-2xl font-bold text-center text-green-700 mb-6">Recuperar Senha (Transportador)</h2>

    <%= form_with url: transportador_password_path, method: :post, local: true, class: "space-y-6" do |f| %>

      <div>
        <%= f.label :email, "E-mail cadastrado", class: "block text-sm font-medium text-gray-700 mb-1" %>
        <%= f.email_field :email, required: true, autofocus: true, class: "w-full border rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-green-500" %>
      </div>

      <div>
        <%= f.submit "Enviar Instruções", class: "w-full bg-green-600 hover:bg-green-700 text-white font-bold py-2 px-4 rounded shadow" %>
      </div>

    <% end %>

    <div class="mt-6 text-center text-sm text-gray-600">
      <%= link_to "Voltar ao Login", new_transportador_session_path, class: "text-green-600 hover:underline font-medium" %>
    </div>
  </div>
</div>
