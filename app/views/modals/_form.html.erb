<%= form_with(model: modal) do |form| %>
  <% if modal.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(modal.errors.count, "error") %> prohibited this modal from being saved:</h2>

      <ul>
        <% modal.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nome, style: "display: block" %>
    <%= form.text_field :nome %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
